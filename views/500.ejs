<%- include('includes/header.ejs')%>

    <% var errorMessage %>
        <div class="jumbotron">
            <h1 class="display-4">程序出錯</h1>
            <p class="lead">
                <% if(errorMessage) { %>
                    <%= errorMessage %>
                        <% } else { %>
                            很抱歉，服務器異常請稍後再試！
                            <% } %>
            </p>
            <hr class="my-4">
            <p>請點擊按鈕返回首頁</p>
            <a class="btn btn-primary btn-lg" href="/" role="button">返回首頁</a>
        </div>
        <%- include('includes/footer.ejs')%>